export type TAttempt = {
  id: number;
  answer: string | undefined;
}

export type TFinalMessage = {
  message1: string;
  message2: string;
}

export type TMinPoints = {
  points: Number;
}

export type TRankingPlayer = {
  name: string;
  points: number;
}

